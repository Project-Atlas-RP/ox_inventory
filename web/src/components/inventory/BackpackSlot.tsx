import React from 'react';

// Intentionally empty placeholder. The dedicated backpack-on-the-left slot was
// replaced by the swap + open header buttons in `BackpackHeaderActions.tsx`.
// Kept as a stub so the component path still resolves if anything imports it.
const BackpackSlotPlaceholder: React.FC = () => null;

export default BackpackSlotPlaceholder;
